# Infrastructure Root

This directory is reserved for native deployment infrastructure, including future Caddy and systemd configuration.

Phase 1 tracks this root with a README instead of an empty sentinel. Do not add deployment files here before the relevant ops phases.
